- `[ ]` **IKControl Implementation**
  - `[ ]` Create `Engine/Core/DataModel/IKControl.h`
  - `[ ]` Create `Engine/Core/DataModel/IKControl.cpp`
  - `[ ]` Implement `apply()` logic (analytic 2-bone solver).
  - `[ ]` Register properties (`EndEffector`, `TargetPosition`, etc.) in `TypeRegistry`.
- `[ ]` **Humanoid Integration**
  - `[ ]` Modify `Engine/Core/DataModel/Humanoid.cpp` to iterate children and find `IKControl`.
  - `[ ]` Call `ik->apply(...)` for each active control and recompute world pose.
- `[ ]` **Build System**
  - `[ ]` Add `DataModel/IKControl.cpp` to `Engine/Core/CMakeLists.txt`.
- `[ ]` **Build & Verify**
